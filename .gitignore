__pycache__/
*.pyc
.env
*.egg-info/
.venv/
dist/
*.log
.DS_Store
node_modules/

# Datos generados masivamente (se regeneran con scripts/generate_data.py)
generated_*.sql

# Course deliverables (never committed to this repo)
*.pdf
*.docx

# Claude AI (Code) — configuration, context and session files
.claude/
CLAUDE.md
CLAUDE.local.md
CLAUDE_Code.md

# Local scratch / ideation notes
IDEA.md
.playwright-cli/

# Project context document (generated, not part of the repo)
contexto.md

# Frontend refactor plan (working doc, not part of the repo)
ACTION-PLAN-FRONTEND.md

# Pending items / known gaps (working doc, not part of the repo)
pendientes.md

# Kubernetes client certificates (machine-specific, opt-in for the control panel)
k8s/certs/*
!k8s/certs/.gitkeep
